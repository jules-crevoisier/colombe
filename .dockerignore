node_modules
.output
.nuxt
.data
.env
.env.*
!.env.example

tests
test-results
playwright-report

# docs/ n'est jamais nécessaire pour construire l'image (ni la doc admin/guide,
# ni le site VitePress public en cours d'écriture par ailleurs) : exclu en bloc.
# La doc admin/guide reste incluse dans l'archive de release (scripts/build-release.mjs
# lit le dossier directement sur disque, indépendamment de ce fichier).
docs

.orchestrator
.deploy
.claude
.git
.gitattributes
.github

*.log
.DS_Store
.build.lock

release
deploy
Dockerfile
.dockerignore
docker-compose.yml
